import 'package:flutter/material.dart';

class Touristscreen extends StatelessWidget
{
  const Touristscreen ({super.key});

  @override
  Widget build (BuildContext context)
  {
    return Scaffold(
   
      body: Center(child:Text('Tourist Places')),
    );
  }
}